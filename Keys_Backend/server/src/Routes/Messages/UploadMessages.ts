export default {
    OverwriteMessage: "The dbUpdatedAt provided is older than the server's, this means you are trying to upload an older database that may be out of sync with the server's. If you'd like to overwrite, then supply overwrite=true as a query parameter."
}